/**
 * config 
 */
var path = require('path');

var config = {
	debug: true,
	port: 3006,
	secret: 'ArJuNa-SuPeR-SeCrEt-KeY-123-789-4560',
	mysql: {
		host: 'localhost',
		username: 'root',
		password: '',
		database: 'Arjuna'
	}
}

module.exports = config